"use client"

import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import Link from "next/link"

export default function Home() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-50 to-indigo-100">
      <Card className="w-full max-w-2xl mx-4">
        <CardHeader className="text-center">
          <CardTitle className="text-4xl font-bold mb-2">
            Welcome to QUAP
          </CardTitle>
          <CardDescription className="text-lg">
            Quick-Commerce Analytics Platform
          </CardDescription>
        </CardHeader>
        <CardContent className="space-y-6">
          <div className="text-center space-y-4">
            <p className="text-muted-foreground">
              Universal analytics for Blinkit, Zepto, and Instamart
            </p>

            <div className="grid gap-4 md:grid-cols-3 mt-6">
              <div className="p-4 rounded-lg bg-blue-50 border border-blue-100">
                <h3 className="font-semibold text-blue-900 mb-2">Sales Analytics</h3>
                <p className="text-sm text-blue-700">
                  Track sales, orders, and revenue across all platforms
                </p>
              </div>

              <div className="p-4 rounded-lg bg-green-50 border border-green-100">
                <h3 className="font-semibold text-green-900 mb-2">Inventory Management</h3>
                <p className="text-sm text-green-700">
                  Monitor stock levels, DOI, and warehouse inventory
                </p>
              </div>

              <div className="p-4 rounded-lg bg-purple-50 border border-purple-100">
                <h3 className="font-semibold text-purple-900 mb-2">Profitability Tracking</h3>
                <p className="text-sm text-purple-700">
                  Analyze costs, margins, and profit by product
                </p>
              </div>
            </div>
          </div>

          <div className="flex gap-4 justify-center mt-8">
            <Link href="/login">
              <Button size="lg">
                Get Started
              </Button>
            </Link>
            <Link href="/register">
              <Button size="lg" variant="outline">
                Sign Up
              </Button>
            </Link>
          </div>

          <div className="text-center text-sm text-muted-foreground mt-6">
            <p>Version {process.env.NEXT_PUBLIC_APP_VERSION || "1.0.0"}</p>
            <p className="mt-1">© 2025 QUAP. All rights reserved.</p>
          </div>
        </CardContent>
      </Card>
    </div>
  )
}
